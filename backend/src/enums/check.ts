export const enum CheckCategory {
    WORD = 'word',
    SENTENCE = 'sentence',
}

export const enum CheckType {
    EXPLICIT = "explicit",
    SENTIMENT = "sentiment",
    POLITICAL = "political",
    SPAM = "spam",
}